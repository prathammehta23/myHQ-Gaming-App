# myHQ-Gaming-App

## Firebase rules deploy

This repo now includes Firebase project wiring for the live project `myhq-gaming-platform`.

Deploy the updated security rules with:

```bash
./scripts/deploy-firebase.sh
```

If the Firebase CLI is not installed, install it first:

```bash
npm install -g firebase-tools
```